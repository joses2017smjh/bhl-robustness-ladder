# Repository master backlog

Every experiment and gallery entry mapped to its code, configuration,
checkpoint, evaluator and evidence, with a classification and the single next
executable action. Classifications: **completed reference**, **active
improvement**, **unresolved failure**, **dependency-blocked**, **retracted**,
**superseded**. A working renderer is not a successful experiment. Numbers in
the gallery and README are starting points; each row states what the evidence
files actually contain.

Status page: [`STATUS.md`](STATUS.md). Mission 7 detail: [`MISSION7_TASKS.md`](MISSION7_TASKS.md).
Folding detail: the linked repository's own `campaigns/*/STATUS.md`.

## Compute plan (declared 2026-09-23)

| Line | Limit | Accounting |
|---|---|---|
| Concurrency | ≤ 8 jobs | this session's `m7-`, `insp-`, `airlock`, `loco-`, `b5-`, `coop-` jobs |
| CPU (`share`, haswell&el8) | as needed within site limits | per-workstream episode counts below |
| GPU (`gpu`/`ampere`, ≤2 concurrent) | ≤ 24 GPU-h eval/render + ≤ 24 GPU-h one justified training | logged per job in `SLURM_JOBS.md` |
| Disk | ≥ 100 GB free; raw traces trimmed | `df` checked at each checkpoint |
| Mission 7 | 101 CPU episodes remaining of 512 | `MISSION7_TASKS.md` |
| Folding repo | none from this session while its driver runs | — |

## Coordination note

`lehome-fold-repro` (IsaacSimFolding, branch `main`) is being executed by its
own driver (`scripts/driver.py`, jobs `lh-v3-*`, campaign
`20260923-recovery-supervision-v3`). This session does not edit that
repository or submit folding jobs while the driver is live; its `REPORT.md`
and `STATUS.md` are consumed read-only and cross-referenced here.


## Backlog by workstream

Columns: task · entry · classification · verified evidence (from the inventory, checked against files) · next action · cost · status. Status vocabulary: TODO · ACTIVE (job id) · DONE (commit) · NOT FUNDED (reason) · BLOCKED (exact prerequisite).

### Navigation PPO and B5 maze/sensing

| task | entry | class | verified evidence | next action | cost | status |
|---|---|---|---|---|---|---|
| NAV-01 | Goal-reaching PPO curriculum | completed reference | Recounted from all 36 JSONs: Approach 380, Corridor 375, Full 379; every per-cell count matches the WEEKEND_RESULTS table (Approach: Both s0 30, Lidar s0 31, Stereo s0 31, rest 32; Corridor: Blind s2 30, Both s0 30, Lidar s0 30, L **Discrepancy:** Numbers reconcile with no discrepancy. Provenance caveats: (1) No clip exists for any 12-DoF recovery checkpoint; the README hero and gallery clips (weekend-ins | Close the eval-noise caveat: add a --keep-corruption flag to scripts/bench/maze_recovery_probe.py (one guarded line around the enable_corruption=False assignment) and re-evaluate the 12 Full model_5997.pt checkpoints with corruption on, same --num-envs 32 --st | ~0.5 GPU-h total (12 evals x ~2-3 min incl. Kit boot) on gpu | TODO |
| NAV-02 | isaac/maze_stereo_fixed | completed reference | Recomputed from event files 2026-09-23 (last-50 mean of 6000): mazefix-stereo s0 0.877 / s1 0.560 / s2 0.886; mazefix-stereop16 1.286 / 1.340 / 1.237; mazefix-bothp16 1.063 / 1.065 / 1.079; blind 0.545 / 0.634 / 1.037; lidar 0.794 **Discrepancy:** (1) Caption '0.88 (was 0.02)' compares corrected seed 0 (0.877) against the pre-fix 3-seed mean (0.021); pre-fix seed 0 itself was 0.001. (2) The corrected 16x1 | Write the missing sidecar docs/gifs/isaac/maze_stereo_fixed.json (run dir, event-file and frame-dir sha256, job ids 21317023/21329137, seed-0 0.877 and 3-seed mean 0.774 vs blind 0.738, pre-fix seed-0 0.001 / mean 0.021) and correct the GIF/GALLERY caption to  | <0.1 CPU-h on the login node (python 3.11 shared venv at /nf | TODO |
| NAV-03 | isaac/mazenav_seed0 | superseded | Recomputed from all 12 event files: Episode_Termination/button_reached last-50 = 0.0 and max = 0.0 in every run; time_out 0.934-0.961; mean_episode_length 480.5-487.3; progress_to_button max 0.0003-0.0005; Metrics/success_rate 0.9 **Discrepancy:** Numbers reconcile. Provenance gaps: (1) results/mazenav_last50.csv has no generating script in the repo; verification here is by independent event-file recomput | Rollout-score the four mazenav-*-s0 model_5999.pt checkpoints under their legacy task ids with scripts/bench/maze_recovery_probe.py (its non-recovery fallback reads termination_manager terms 'button_reached' and 'dead_end', both present in the legacy MDP) --nu | ~0.2 GPU-h (4 x ~2-3 min incl. Kit boot) on gpu/ampere/dgxh, | TODO |
| NAV-04 | isaac/terrain_sensors | completed reference | All 15 pre-fix values recomputed exactly from event files (blind 0.545/0.634/1.037; lidar 0.794/0.814/0.971; stereo 0.001/0.000/0.061; P16 1.027/1.139/1.207; bothP16 1.203/0.972/1.170), plus the corrected rows (entry 2). 200 frame **Discrepancy:** (1) The GIF's two stereo panels are renders of retracted upward-camera policies with their retracted per-clip terrain-level labels; GALLERY states this, but the | Re-render the two stereo panels from the corrected checkpoints so the clip matches the table: point the stereo_p4 and stereo_p16 record globs in slurm/inner/maze_video.sh at mazefix-stereo-s0 and mazefix-stereop16-s0, submit slurm/97_maze_video.sbatch with CLI | ~0.3 GPU-h (21247917 took 17:36 for four arms; two arms ~10  | TODO |

### Inspection and multi-robot airlock

| task | entry | class | verified evidence | next action | cost | status |
|---|---|---|---|---|---|---|
| INS-01 | Inspection mission GIF/MP4 | completed reference | inspection-maze-gate.json verified: ordered/reactive seeds 0,1,2 success true, completion 17.24 / 17.44 / 17.28 s, stations 2, waypoints 5, wall_contact_steps 0, path 6.90/6.90/6.97 m; ordered/reactive_dropout (p=1.0) seeds 0-2 mi **Discrepancy:** (1) The hero 17.36 s clip and the wrong-branch clip are interactive renders on desktop allocations (21358937 / 21367413), not the gated Slurm run: single seed,  | Multi-seed + stress extension of the existing fixed layout, executable without code edits: submit two CPU jobs through scripts/submit_weekend_job.py --cpu (or sbatch slurm/weekend_cpu.sbatch directly) with --constraint='ivybridge&el8' to match cn-b01/b02/b03:  | Measured: 12 episodes in 3:59 wall on 2 cores (~20 s wall pe | TODO |
| INS-02 | Wrong-branch control | completed reference | Gate JSON: wrong_branch/reactive seeds 0,1,2 -> success false, failure dead_end_entered, elapsed 7.04 / 7.04 / 6.96 s, stations_completed 1 (inspect_A at ~3.8 s), waypoints_completed 1, wall_contact_steps 0, path ~2.55-2.59 m. Vid **Discrepancy:** (1) Clip is an interactive single-seed render on allocation 21367413 (Sep 20), a day after the gate job; its JSON has controls_complete=false by construction. ( | Fold into the same multi-seed job as the nominal entry (it is already produced by --routes ordered wrong_branch); additionally add to the bench's summary a wrong_branch success-rate entry and make --gate require wrong_branch rate == 0 with failure == dead_end_ | Zero marginal Slurm cost beyond the nominal job (a): the 20  | TODO |
| INS-03 | Three-robot airlock | completed reference | team3.json: coordinated seeds 0-4 all success, completion 30.76 / 30.44 / 30.28 / 30.48 / 30.24 s, release_s 4.68/4.44/4.52/4.60/4.60, stations 3, members_crossed 3, 0 robot/wall contact steps, min separation .467/.462/.470/.465/. **Discrepancy:** (1) 30.68 s hero clip is an interactive seed-0 render (allocation 21358937, 45 s horizon), identical to results/team_airlock_trio_probe.json; the gated Slurm se | Multi-seed + stress extension, executable now without code edits, via scripts/submit_weekend_job.py --cpu (slurm/weekend_cpu.sbatch) with --constraint='ivybridge&el8' and unique --cache-dir per job: (a) crew 3, --seeds 20 --seed-start 5 --seconds 60 --modes co | Measured: crew 3 15 episodes in 13:52 wall on 4 cores (~55 s | TODO |

### Locomotion, terrain and sensor robustness

| task | entry | class | verified evidence | next action | cost | status |
|---|---|---|---|---|---|---|
| LOC-01 | dr_pair | completed reference | Recomputed from CSVs: dr-off 21/90=0.2333, dr-default 0/90, dr-s0.5 1/90=0.0111, dr-s1.5 0/90, dr-aggressive 15/90=0.1667 (matches flat_summary and REPORT §1 exactly). Checkpoint table matches exactly (30 episodes/cell). Both clip **Discrepancy:** dr-default-s1 (job 20935266_4) FAILED at 2:40:43 and stopped at 5495 iterations (55 checkpoints) yet is counted as a full 6000-iteration seed in the 3-seed tabl | Generate a JSON sidecar for dr_pair.gif recording the two source MP4 sha256s, the CSV rows (results/raw/dr-default-s0.csv, dr-off-s0.csv strafe rows) and outcomes, and note the 5495-iteration seed in REPORT §1. | ~0.05 CPU-h on the login node or share partition with /nfs/h | TODO |
| LOC-02 | push_pair | completed reference | demo CSVs confirm 0/6 vs 3/6 (n=6, single policy seed each, 12 s episodes, 3 pushes). Manifest confirms adaptive push_levels pinned at cap 1.0 and uncapped max 1.79/1.86. flat_summary push rows are scored at 0.4 m/s and the dr-def **Discrepancy:** The launcher that produced results/demo and results/raw/demo-*.csv is not in the repo and predates the ledger; labels 'nopush-baseline' do not identify which ch | Score dr-default s0-s2 and push-adaptive s0-s1 (plus push-c0.2/0.4/0.6) under PUSH_SPEED=0.5, EPISODE_S=12, N_SEEDS=5 via slurm/inner/eval_one.sh and write results/raw/<label>__p0.5.csv. | ~0.5-1 CPU-h total (about 3 min per policy cell on CPU); no  | TODO |
| LOC-03 | terrain_pair | completed reference | Every retention cell recomputed from results/terrain CSVs matches REPORT §3 exactly (n=90, smooth n=60). Demo CSVs confirm 0/6 vs 3/6. Event-file terrain levels: bumpy final 1.4431/1.4465/1.4171, flatfill 1.3928/1.2904 (last-50 1. **Discrepancy:** results/demo clip launcher and the 'flatDR-baseline' checkpoint identity are unrecorded (same gap as push_pair). terrain-flatfill s0/s1 were never scored in MuJ | Run the 21_terrain_sweep/22_terrain_high protocol on terrain-flatfill-s0/s1 (export if exported/deploy.yaml is missing, then 8 difficulties x 30 episodes each) and add the rows to results/terrain_retention.csv. | ~1 GPU-h on the gpu partition (Isaac export inside bhl.sif v | TODO |
| LOC-04 | arms_dr_pair | completed reference | results/arms CSVs: arms-dr0.0 0/60, arms-dr1.0 0/60 on flat; demo_arms/dr both 0/6. Matches REPORT §1 footnote. GALLERY lists it as 'works' although the clip shows no failure by design. **Discrepancy:** GALLERY caption 'At d=1.0 the humanoid falls 11.7% where the biped falls 37.8%' belongs to the terrain sweep, not this flat strafe clip. Only 2 training seeds ( | Caption fix: the '11.7 % vs 37.8 %' sentence belongs to the terrain sweep, not this flat clip. Third seeds **not funded** (~10–12 GPU-h for a caption qualifier). | DOC | TODO |
| LOC-05 | arms_push_pair | completed reference | n=60 CSVs confirm push-trained 0.15 vs DR-only 0.10: the push-trained 22-DoF arm falls MORE than the no-push control. In the filming run itself arms-push-s0 fell 2/6 and arms-dr1.0-s0 1/6; the GIF shows the single command where th **Discrepancy:** The clip labels and the GALLERY caption invert the repo's own n=60 result (push-trained 9/60 vs control 6/60); '0.2 m/s free' is a biped training-side finding t | **Caption inverts the n=60 result** (push-trained 0.15 vs control 0.10): relabel GALLERY and sidecar; remove the transplanted '0.2 m/s free' sentence. | DOC | TODO |
| LOC-06 | arms_terrain_pair | completed reference | All 20 cells recomputed from results/arms CSVs match REPORT §3 exactly (e.g. dr1.0 d0.80 5/60=0.0833, d1.00 7/60=0.1167; terrain 0/60 at every d). Demo CSVs confirm the clip outcome. **Discrepancy:** Two training seeds only; the 22-DoF-vs-biped comparison (0.117 vs 0.378 at d=1.0) mixes n=60 against n=90. No sidecar. | State n=60 vs n=90 and two seeds in the caption. Third seed **not funded**. | DOC | TODO |
| LOC-07 | multi_race | completed reference | Log confirms exactly one fall (no-randomization at 4.8 s) in a single 12 s rollout; the other three stayed upright. n=1 per policy; no denominator recorded anywhere in results/. **Discrepancy:** The clip is illustrative only: no results file records the race outcome, and the bulk tables were scored under different push settings (0 or 0.4/0.5). Pre-ledge | Re-run render_multi.py with --seconds 12 --push 0.45 --world flat for 5 seeds (no GIF), writing per-robot fall/peak-x rows to results/multi_race_seeds.csv. | ~0.1 CPU-h (5 x ~1 min) on share with the venv; GPU only if  | TODO |
| LOC-08 | multi_lab | completed reference — 5-seed rerun DONE | The 21004154 log traversal tables match REPORT §8 row for row. Single rollout per policy per morphology, one seed. **Discrepancy:** The only evidence is a Slurm log outside the repo; nothing under results/ records the traversal. GALLERY describes the course as 'plank, beam and ramp'; the sce **Rerun 2026-09-23 (`21402037`/`21402038`, `results/lab-traverse-20260923/lab_traverse_5seed.json`):** 22 DoF finishes 1/5, 0/5, 2/5, 2/5 (randomized, no-rand, push, terrain); 12 DoF 0/5, 0/5, 0/5, 2/5. REPORT §8 'only the 22-DoF model crosses' corrected to 'more often, neither reliably'. | Course is carpet/cable/threshold/ramp, not 'plank, beam and ramp' — relabel; 5-seed lab_traverse CSV. | 40 CPU rollouts spent | DONE |
| LOC-09 | depth_pair | completed reference | Event files confirm depth-bumpy final 1.6010/1.5983 (last-50 1.5902/1.5983) vs bumpy 1.4431/1.4465/1.4171. The validation and throughput numbers have no surviving evidence file: results/depth_bench.txt is absent, the two depth-cli **Discrepancy:** Clip is a blind policy with an unconsumed depth overlay; sim2sim of the depth policy never done (no ONNX); depth validation/throughput claims are unbacked in re | Re-run slurm/inner/depth_bench.sh (depth_validate.py --res 64 --num_envs 16 and depth_raycast.py at 2048/4096 envs) with BENCH_OUT=results/depth_bench.txt so the finite-pixel, relative-error and env-steps/s numbers exist as an evidence file. | ~0.25 GPU-h on the gpu partition (a40/rtx8000 constraint), b | TODO |
| LOC-10 | ice_pair | retracted | Event files confirm the retracted levels (last-300 means depth 1.5192/1.5188, blind 1.3392/1.4084, visible 1.3392/1.4487 -> 1.519/1.374/1.394). Probe log confirms UNREACHABLE (0.044). Additionally verified from code: the depth cam **Discrepancy:** GALLERY ('blind vs depth on flush friction patches') and the ledger describe the MuJoCo clip as being on friction patches; the harness has no ice world, so both | GALLERY says friction patches; the MuJoCo harness has no ice world. Relabel; copy the UNREACHABLE probe verdict to its own file so the retraction evidence cannot be overwritten again. | DOC | TODO |
| LOC-11 | ice_pair_placed | active improvement | Recomputed from the six event files: blind 2.5763/2.6055, depth 2.8541/2.9837, visible identical to blind to four decimals (bitwise-duplicate runs) -- CSV matches exactly. Last-300 means give the same sign (2.580/2.598 vs 2.839/2. **Discrepancy:** (1) HEAD ice_export.sh/ice_render.sh cannot reproduce the placed export/clip (they name ppo-ice-blind-s0/depth-s0 and deploy_ice_blind/depth.yaml; the 21352981  | GALLERY says friction patches; the MuJoCo harness has no ice world. Relabel; copy the UNREACHABLE probe verdict to its own file so the retraction evidence cannot be overwritten again. | DOC | TODO |

### Cooperative lift, carry and cube-to-shelf

| task | entry | class | verified evidence | next action | cost | status |
|---|---|---|---|---|---|---|
| COOP-01 | squat_pick | completed reference | Clip and mp4 exist; render_pick.py confirms base pinned in orientation/xy and bin parented to hands, so it demonstrates kinematic reach only, not balance or contact **Discrepancy:** None in the claim itself; note that the base is pinned and the object is welded to the hands, so it is not evidence of a feasible dynamic grasp | none; keep as the kinematic control | 0 | — |
| COOP-02 | carry_2 | superseded | coop_clips_pov.csv confirms 7.8 cm / 97.7 % / fell 0 / 300 steps for seed 11. pov-gifs-21066607.out confirms 12-seed median 5.5 cm, mean 5.3 cm, 6/12 upright. 41 cm base sink confirmed by pick_seed 'base sank 41.1-41.8 cm' columns **Discrepancy:** (1) results/coop_clips.csv still carries the superseded Aug-22 seed-0 rows (5.3 cm, fell 1) that no longer match the published gif; the matching rows are in coo | none; welded-hand line closed by FINDINGS ('a property of the model'); do not re-render | 0 | — |
| COOP-03 | carry_3 | superseded | CSV confirms pair 7.8 cm peak with 97.7 % in-pinch and solo 5.7 cm peak with 0 % in-pinch, both upright. The solo robot lifts nearly as much without ever forming a pinch, i.e. the crate is being nudged/braced by a forearm, not coo **Discrepancy:** GALLERY caption describes carry_3 as 'learned vs scripted' but the clip is pair + solo control (render_carry docstring); the solo control's 5.7 cm (>= pair held | none | 0 | — |
| COOP-04 | carry_4 | superseded | Confirmed by CSV: both pairs in-pinch 98 %, peaks 5.0 / 1.9 cm, no falls **Discrepancy:** none beyond missing sidecar and the stale coop_clips.csv rows | none | 0 | — |
| COOP-05 | carry_cube_pov | superseded | 7.8 cm / 97.67 % / fell 0 confirmed in coop_clips_pov.csv. 6/12 upright and max 7.8 cm confirmed in 21066607 log; that log's median/mean are 5.5 / 5.3 cm. **Discrepancy:** Median/mean quoted in FINDINGS (5.1/5.2) differ from the retained sweep log (5.5/5.3); a second, fall-truncated sweep is implied but not retained. FINDINGS' own | none | 0 | — |
| COOP-06 | carry_ball_native_pov | retracted | CSV/log confirm seed 0: peak 0.0 cm, 2 % in-pinch, fell, frozen at 0.8 s. Isaac final episode length 37-38 steps of 200 (log) is consistent with a policy that mostly falls. The 20.9 cm Isaac figure, the '0/6 seeds, 0.72 s' sweep a **Discrepancy:** '0/6 seeds, mean fall 0.72 s' and the cross-scene table exist only in prose; only one seed is in evidence. No sidecar. | none (retracted in REPORT Corrections); if a number is ever needed, run coop_sim2sim.py --run-dir <ball-staged> --seeds 8 --crews 2 to a CSV | ~0.2 CPU-h on share (MuJoCo, no GPU) if re-scored; otherwise | — |
| COOP-07 | carry_ball_transfer_pov | superseded | Seed-4 clip row confirms 9.1 cm peak, fell. Retained sweep log confirms 11/12 fall at 1.04-1.84 s (mean ~1.5 s) and 1/12 upright, but its peak column (untruncated at the fall) reads median 13.0 / mean 14.0 / max 27.6 cm; the 3.1 / **Discrepancy:** Retained log medians (13.0/14.0 cm) differ from the documented 3.1/3.7 cm because of the peak-truncation change; the doc numbers cannot be reproduced from any r | none | 0 | — |
| COOP-08 | carry_ladder_pov | superseded | Log confirms 12/12 seeds at 0.0 cm, upright 12 s, closest 0.393 m. The ~34 cm sink is cited to one-off probe 21067131, not retained. **Discrepancy:** GALLERY says 18 seeds; the retained log and FINDINGS say 12. coop_clips_pov.csv also keeps a pre-fix row (closest 1.49 m) from the wrong-contact-point render. | GALLERY says 18 seeds; the retained log has 12 — relabel. | DOC | TODO |
| COOP-09 | carry_vision_swap_2 | superseded | Direction confirmed from coop_vision_sim2sim.csv: swap held 0.4-0.7 cm and fell 0.94-1.00 across crews vs blind held 4.0-4.4 cm, fell 0.12-0.25. The exact doc ranges (pinch 0.42-0.91 etc.) do not reproduce from per-crew means (bli **Discrepancy:** Doc ranges are not the per-crew aggregates of the CSV; the head of coop_depth_validate.txt shows a FAILED attempt that was later superseded (cite the tail). No  | none | 0 | — |
| COOP-10 | carry_vision_swap_3 | superseded | confirmed: pair fell, peak 5.7 cm during topple, 7.7 % in-pinch **Discrepancy:** none beyond missing sidecar | none | 0 | — |
| COOP-11 | carry_vision_swap_4 | superseded | CSV confirms both pairs fall, peaks <= 3.2 cm **Discrepancy:** Rendered under the pre-fix depth ramp; not comparable to the other POV strips (why it is unused) | none (delete or leave unused) | 0 | — |
| COOP-12 | carry_vision_both_2 | superseded | Confirmed: in-pinch 0.000, fell 1.00 in every crew configuration in the CSV **Discrepancy:** doc 'fell 0.88-1.00' vs CSV pair means 1.00 (0.75 only for the solo control). No sidecar. | none | 0 | — |
| COOP-13 | carry_vision_both_3 | superseded | confirmed: all three robots fall, 0 cm **Discrepancy:** old depth ramp; unused | none | 0 | — |
| COOP-14 | carry_vision_both_4 | superseded | confirmed by CSV (both pairs fell, 0 cm, 0 % in-pinch) **Discrepancy:** none beyond missing sidecar | none | 0 | — |
| COOP-15 | occlusion_s0_lifted_pov | unresolved failure | Checkpoints exist and the env.yaml difference between the two s0 dirs confirms which one was truly occluded. None of the MuJoCo numbers (0.251 m, 0 %, 0.8 s) or the Isaac numbers (0.1313, ep len 51) are in a retained machine-reada **Discrepancy:** All quantitative claims are prose-only; no evidence file, no job receipt, no sidecar. The 16k run's mean episode length 51 (vs ~195) means the 'lift' happened i | Score both occluded s0 dirs and s1/s2 with scripts/bench/coop_sim2sim.py --seeds 8 --crews 2 3 --csv results/coop_occluded_sim2sim.csv so the claim has a file; no training | ~0.3 CPU-h on share (4 CPUs), no GPU, existing venv | TODO |
| COOP-16 | occlusion_s1_flat_pov | unresolved failure | Checkpoint exists; numbers are prose-only, not in any results file **Discrepancy:** Same evidence gap as s0 | included in the occluded coop_sim2sim CSV run above | included (~0.3 CPU-h total) | TODO |
| COOP-17 | isaac/cubetoshelf_upright_welded | retracted | Run dir, checkpoint and mp4 exist. Log confirms success 0.0000 and ep len ~243-263 at 6,604 iters. Retraction stands (render_probe JSON body_z_min -0.80). Subsequent v2stand rerun with the current native_quat default spawn (job 21 **Discrepancy:** GALLERY quotes 258.9/+10.73 while the ledger quotes 263.2/+12.00 for the same run (different readout windows). spawn_quat_probe.json says the current configured | Run the spawn-stability diagnostic (see notes): 200 zero-action/PD-hold steps of TaskV2-BHL-CubeToShelf-Blind-v0 and -Grip-Blind-v0 at 4 envs on the current default spawn, logging per step tilt (_tilt_from_quat), n_bodies_below_z0, ankle vs shoulder z, fallen  | ~0.5 GPU-h, partition gpu,dgxh,ampere (constraint a40/rtx800 | BLOCKED |
| COOP-18 | cubetoshelf_upright_gripper | retracted | Run, checkpoint and mp4 exist; ledger confirms success 0, fallen 0.893, lift_height reached 0.098 (curriculum promoted off the floor while buried). No standing rerun of the gripper cube cell exists after the native_quat default ch **Discrepancy:** none beyond the retraction; the gripper cube cell was never rerun on the current spawn | same spawn-stability diagnostic as the welded cell (it is the gripper cell that would train next) | included in the ~0.5 GPU-h diagnostic | BLOCKED |
| COOP-19 | isaac/cubetoshelf_gripper | unresolved failure | Log confirms ep len 447 and success 0 for this cell (427.7 is the 6-cell mean). Survival gain is confounded by the buried spawn (FINDINGS caveat: episodes ended inside the extrusion window for welded arms). **Discrepancy:** GALLERY '427 steps' is the 6-cell mean, this cell is 447; the gain is not separable from surviving the spawn | do not retrain; the spawn diagnostic above must pass first | 0 now; 400-iter gripper cube probe after the diagnostic ~0.6 | BLOCKED |
| COOP-20 | cubetoshelf_welded | superseded | Checkpoint and mp4 exist; the ~8-step figure is a 6-8 cell mean from event files (no per-cell retained file); consistent with the extrusion window **Discrepancy:** none material; superseded first by the gripper cell and then by the (retracted) corrected-spawn reruns, and again by the v2stand reruns that also die in 5 steps | none | 0 | — |
| COOP-21 | coop_crew_3_4 | dependency-blocked | Confirmed: gate FAIL x4 and import error; no checkpoint; crew_diag isolates the fault to the Hydra round trip of generated configclass fields **Discrepancy:** none; the docs do not claim a crew result | none until the pair task is valid; then fix the generated configclass annotation mismatch and rerun slurm/62_crew_gate.sbatch | code fix + ~1 GPU-h gate (gpu,dgxh,ampere, v51 container) wh | — |

### Cloth-sort proxies and the linked folding study

| task | entry | class | verified evidence | next action | cost | status |
|---|---|---|---|---|---|---|
| CLO-01 | isaac/cloth_sort_free_base_shirt | completed reference | VERIFIED: isaac_c0_bal2_shirt.json success_rate 0.75 = 6/8, fall_rate 0, nonfinite 0; sock 1.0 = 8/8; jacket 1.0 = 8/8; sum 22/24. Job logs and sacct (all COMPLETED, ampere, A40 for clip) agree. Balance v1 predecessor also verifie **Discrepancy:** (1) No GIF sidecar JSON and no committed frames->GIF assembly script; provenance stops at results/clips/frames/cloth_c0_bal2_shirt (192 frames) and the clip JSO | Replicate 21330402 at two more seeds to bound variance before citing 6/8 as a rate: sbatch slurm/95e_cloth_isaac_eval.sbatch with EVAL_GARMENT=shirt EVAL_EPISODES=8 and eval_isaac.py --seed 1 / --seed 2 (requires passing --seed through cloth_isaac_eval.sh, a o | ~0.15 GPU-hour total (2 jobs x ~3 min each + v60 boot), part | TODO |
| CLO-02 | isaac/cloth_sort_free_base_jacket | completed reference | VERIFIED: isaac_c0_stance_jacket.json success_rate 1.0 over 8 episodes (fall 0, nonfinite 0); isaac_c0_bal2_jacket.json 1.0 over 8 (fall 0, nonfinite 0); clip JSON success 1.0, fall 0. sacct: all COMPLETED. **Discrepancy:** The published clip is a balance-v1 recording (job 21329392 ran at 16:03 PDT; balance v2 commit 42ba537 is 17:16 PDT); GALLERY.md states this correctly but the C | None required for the reference. If a v2-labelled clip is wanted, rerun the clip job under current HEAD (BHL_CAMERA_CLIP=1 EVAL_GARMENT=jacket) and add a sidecar JSON with frame-dir and output sha256; otherwise leave as is. | ~0.05 GPU-hour (one ~1 min job + boot), partition gpu/ampere | — |
| CLO-03 | isaac/cloth_sort_fixed_base | completed reference | VERIFIED: three JSONs report success_rate 1.0 with n_episodes 16, 8, 8 (32/32), mean_sweeps 1.0, invalid_trajectory_rate 0.0; task field ClothSort-BHL-RigidFixedBase-Oracle-v0, rung C0F; sacct all COMPLETED on ampere. Note these p **Discrepancy:** None in the numbers. Provenance gaps: no GIF sidecar/hash and no committed frames->GIF script; 'mean_sweeps 64' in the clip JSON is a frame count. Scope caveat  | None; superseded in importance by the free-base runs. Keep as the manipulation-only control. | 0 (no rerun proposed); a rerun would be ~0.1 GPU-hour on amp | — |
| CLO-04 | cloth_sort Newton cloth rungs | unresolved failure | VERIFIED against JSONs: shirt/sock success_rate_finite 1.0 (n=4 each); jacket_mat nonfinite 4, finite 0.0 while headline success_rate 1.0 (a blown-up episode, as the doc says); c2_hold_dt005 nonfinite 2/2 success 0; c5_posefix_ful **Discrepancy:** docs/CLOTH_SORT.md status header says these cells 'stay unqueued until there is a new physics idea' - consistent with the files. The C2F shirt/sock 4/4 is a pin | Do not requeue Newton free-base/jacket cells without a solver hypothesis (doc rule). The one executable step that changes a classification is a C5 Mode B diagnostic of the 2 missed garments: rerun 21353130 with trace on all 4 episodes (eval_isaac.py currently  | ~0.15 GPU-hour (one ~7 min job), partition gpu/ampere, v60 c | BLOCKED |
| CLO-05 | folding: 12-cell matched class evaluation | dependency-blocked | VERIFIED episode by episode from the JSONs: baseline pant_short 24 saved, 8 successes; adapt1 pant_short 24 saved, 3; the three 0/24 cells each have 24 saved, 0; partial cells saved 6, 4, 2, 14, 6, 2, 2 episodes with 0 successes,  **Discrepancy:** (1) The two blockers are only partly resolved and only in the sibling: the scorer index crash (particle 11029 in a 10869-particle mesh, unwelded-mesh remap) was | Complete the matrix as one new campaign, process-isolated per garment: extend bhl scripts/cloth/eval_fold.py (or a campaign copy under lehome-fold-repro/campaigns/<date>-class-matrix/) to run one Isaac process per garment (12 boots per cell) with PYTHONPATH po | ~30 GPU-hours for all 12 cells (~2.5 GPU-h per 24-episode ce | BLOCKED |
| CLO-06 | folding: SmolVLA raster adaptation seeds 0 and 1 | unresolved failure | VERIFIED: checkpoint dirs and best.json exist for both seeds; sacct 21359522 COMPLETED dgxh-1 16:23, 21359530 COMPLETED dgx2-1 27:05; task results as in the matrix entry. The sibling's later campaigns (horizon pilot, closed-loop v **Discrepancy:** Training completion and lower validation loss were initially reported as status; the docs correctly retract that as evidence of folding. bhl docs do not note th | Do not retrain this recipe. If the adapted checkpoints are to be kept in the story, include them as the third arm of the single-campaign 12-cell rerun above; otherwise mark them archival. The sibling's recovery-supervision v3 (successful continuations at stude | 0 additional if archived; ~10 GPU-h (4 classes x 2.5 GPU-h)  | BLOCKED |
| CLO-07 | folding: rendering/media dependency block | superseded | sacct: 21360435 FAILED 0:36 cn-gpu5; 21367715 COMPLETED 3:51 cn-gpu7; BUT 21360436_[1-15%1] and 21360437 show 'CANCELLED by 19646' (0:00, never assigned) - they are no longer preserved/pending. Rollout outputs and all five bhl GIF **Discrepancy:** bhl docs/CLOTH_FOLDING_WEEKEND.md, docs/GALLERY.md ('New folding-policy media is dependency-blocked'), lehome-fold-repro/SESSION_STATUS.md and docs/MEDIA_GATE_2 | Update the bhl and sibling status docs to record the cancellation and the 21367715 replacement; only if new media is still wanted, submit a replacement 15-episode array from campaigns/20260920-media-root-fix with the repaired asset root and pinned checkpoint h | ~8 GPU-hours max for 15 x 30-min single-GPU jobs (one at a t | TODO |
| CLO-08 | folding: horizon pilot H50/H10/H5 | completed reference | VERIFIED from REPORT.md table (Valid 8/8/8, Invalid 0, ever 4/0/0, settled 4/0/0). Per sibling CLAUDE.md the same baseline later scored H10 2/8 in v2 on the same seeds, i.e. run-to-run non-determinism of +-2/8. **Discrepancy:** bhl docs/FOLDING_NEXT_EXPERIMENTS.md still describes this as a proposed, not-run experiment; it has been run with a negative result. Not comparable to the histo | Update bhl FOLDING_NEXT_EXPERIMENTS.md / README to reference the executed pilot; no rerun. | 0; the pilot used 24 x ~5 min GPU tasks (~2 GPU-h) on gpu,am | TODO |
| CLO-09 | folding: closed-loop AWR training v2 | active improvement | Read directly from REPORT.md/STATUS.md (driver-written); not independently recomputed from per-row CSVs. Consistent with the sibling CLAUDE.md orientation. **Discrepancy:** None internal. bhl-robustness-ladder docs (README, CLOTH_FOLDING_WEEKEND.md, FOLDING_NEXT_EXPERIMENTS.md, PUBLICATION_HANDOFF) predate and do not mention these  | Let the v3 driver finish attempt 2 (no manual submission); then, if a candidate passes screen (H10 >= 4/8), run the preregistered confirm (2x H10 + H50) and untouched final set. All changes live in lehome-fold-repro/campaigns/20260923-recovery-supervision-v3/, | Within v3's remaining budget: <= 65 GPU tasks / 36.9 GPU-hou | BLOCKED |
| CLO-10 | folding: MuJoCo idealized towel-fold physics gate | completed reference | Consistent with SLURM_JOBS.md and WEEKEND_CAMPAIGN.md; gate JSON and 8 npz files present (numbers not recomputed here). **Discrepancy:** None; the docs already state it contains no humanoid or gripper and is not evidence of robot folding. | None. | 0 (rerun would be ~0.1 CPU-hour on partition share with /nfs | — |

### Mission 7 (independent workstream; detail in `MISSION7_TASKS.md`)

| task | entry | class | verified evidence | next action | cost | status |
|---|---|---|---|---|---|---|
| M7-01 | Route gate (Doors ≥16, Transport ≥16) | unresolved failure | candidate 1: Doors 1/16 → 1/16, Transport 2/16 → 4/16 on fresh validation 16–31, +2/−0, underpowered | raised-plate crossing that holds the ten-fall replay at 10/10 | new budget line needed | BLOCKED: crossing composition fails replay 7–9/10 |
| M7-02 | Privileged Approach gate | completed reference | 62/64, 0 falls, 16/16/16/14 (`21401966`, test split, tuned there); **held-out −x 21/23 vs baseline 12/23**, +10/−1, p = 0.012 (`21402017–018`) | — | 46 CPU episodes spent | DONE |
| M7-03 | Sensor-only and four-sensor studies | dependency-blocked | — | — | — | BLOCKED: route gate (M7-01) |

### Stale statements in untracked user drafts (left untouched, 2026-09-23)

`docs/FOLDING_NEXT_EXPERIMENTS.md` still calls the horizon pilot a proposed
experiment (it ran: H50 4/8, H10 0/8, H5 0/8), and
`docs/PUBLICATION_HANDOFF_2026-09-20.md` describes the media array as
dependent/pending (it is CANCELLED). Both files are the user's uncommitted
drafts and are preserved as-is; the tracked `CLOTH_FOLDING_WEEKEND.md` and
`GALLERY.md` carry the corrections.

### Not funded (recorded decisions, 2026-09-23)

- Ice no-ice control (~32 GPU-h): gated behind the exposure probe (LOC ice_pair_placed).
- Third seeds for `arms_*_pair` (~10–12 GPU-h each): the gap is a caption qualifier, fixed in text.
- Folding 12-cell matrix (~30 GPU-h): the linked repository's own driver owns folding compute; its v2 closed negative and v3 is mid-protocol.

